import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free KDP Interior Template — Ready-to-Format Book Files for Paperback",
  description:
    "Download free KDP-ready interior templates for common trim sizes. Pre-set margins, bleed, and page setup so you can start formatting without the guesswork.",
};

export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
