/**
 * KDP PDF Checker (Print Ready Check) — max upload size for presigned R2 PUT
 * and enqueue validation. Keep in sync with product copy and FAQ.
 */
export const CHECKER_MAX_UPLOAD_MB = 100;
export const CHECKER_MAX_UPLOAD_BYTES = CHECKER_MAX_UPLOAD_MB * 1024 * 1024;
