# KDP Preflight frontend — API base URL
# Copy to .env and set your backend URL (no trailing slash).
# For local dev with backend on port 8000:
VITE_API_URL=http://localhost:8000

# If using Vite proxy (see vite.config.ts), you can use relative URLs:
# VITE_API_URL=
